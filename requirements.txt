# ===== Core runtime =====
opencv-python==4.12.0.88
ultralytics==8.3.172
deep-sort-realtime==1.3.2
numpy>=1.24
pandas>=2.0
pillow>=10.2.0

# ===== For Depth Anything + HF downloads =====
huggingface_hub>=0.23.0
timm>=0.9.16
einops>=0.7.0

# ===== IMPORTANT: PyTorch =====
# Do NOT pin torch/torchvision here; install them separately from pytorch.org
# so they match each user's GPU/CPU and CUDA version.
# Example (CPU-only):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Example (CUDA 12.1 — adjust if different):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ===== IMPORTANT: Depth Anything =====
# Not on PyPI. Clone the repo and add it to sys.path in your script:
#   git clone https://github.com/LiheYoung/Depth-Anything.git C:\tools\Depth-Anything
# In Python:
#   import sys; sys.path.append(r"C:\tools\Depth-Anything")
#   from huggingface_hub import hf_hub_download
#   from depth_anything.dpt import DepthAnything
#
# Choose backbone & checkpoint (first run downloads and caches):
#   repo_id for vitl: LiheYoung/depth-anything-large-hf
#   repo_id for vitb: LiheYoung/depth-anything-base-hf
#   repo_id for vits: LiheYoung/depth-anything-small-hf
